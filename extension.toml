id = "ios-runner"
name = "iOS Runner"
version = "0.3.9"
schema_version = 1
authors = ["buds520"]
description = "Build and run iOS Xcode projects in Zed (xcodebuild). Bundled ios-runner CLI installs on first load — no cargo or manual download. Requires macOS and Xcode."
repository = "https://github.com/buds520/ios-runner"

[context_servers.ios-runner]
name = "iOS Runner"
