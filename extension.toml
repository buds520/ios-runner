id = "ios-runner"
name = "iOS Runner"
version = "0.3.10"
schema_version = 1
authors = ["buds520"]
description = "Build and run iOS Xcode projects from Zed Agent through a local MCP server. Downloads the matching ios-runner release binary on demand. Requires macOS and Xcode."
repository = "https://github.com/buds520/ios-runner"

[context_servers.ios-runner]
name = "iOS Runner"
